build:
  docker:
    web: Dockerfile
run:
  web: sed -i "s/{{PORT}}/$PORT/g" vconfig/vault.json && cat vconfig/vault.json && cp vconfig/vault.json vconfig/nconf.json && echo "Test vault server" && cat vconfig/vault.json