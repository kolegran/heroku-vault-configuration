build:
  docker:
    vault: Dockerfile
release:
  image: vault
  command: docker run --name=heroku-vault --cap-add=IPC_LOCK -e VAULT_LOCAL_CONFIG='{"backend": { "file": { "path": "/vault/file" } }, "default_lease_ttl": "168h", "max_lease_ttl": "720h" }' vault server