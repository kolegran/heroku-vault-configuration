build:
  docker:
    vault: Dockerfile
release:
  image: vault
  command:
    - aptitude update && aptitude upgrade && aptitude install curl && docker run --name=heroku-vault --cap-add=IPC_LOCK vault server