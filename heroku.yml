build:
  docker:
    web: Dockerfile
run:
  web: ./heroku-exec.sh && sed -i "s/{{PORT}}/$PORT/g" vconfig/vault.json && export VAULT_API_ADDR="http://127.0.0.1:$PORT" && vault server -config=vconfig/vault.json