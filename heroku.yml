build:
  docker:
    vault: Dockerfile
release:
  image: vault
  command: 
    - docker run --cap-add=IPC_LOCK --name=heroku-vault -p 8200:8200 -e VAULT_ADDR="https://kolegran-vault.herokuapp.com" -e VAULT_DEV_ROOT_TOKEN_ID=root -v /vault/logs:/vault/logs -v /vault/data:/vault/data vault